HeartKit Vitals Demo
Copyright (c) 2026, Ambiq

Ambiq-authored code in this repository is licensed under the BSD 3-Clause
License; see LICENSE. The firmware binaries built from this repository also
include third-party components that are licensed separately, and the licenses
and notices for those components are reproduced in THIRD-PARTY-NOTICES.md,
which ships with every release package. The AS7058 sensor driver is
proprietary ams-OSRAM software; it is provided in binary form only, as part of
the prebuilt firmware, under Ambiq's agreement with ams-OSRAM, and its source
is not part of this repository. Building the firmware from source therefore
requires access to the private `nsx-as7058` module referenced by `nsx.lock`.

Hand-written Ambiq source files under `config/`, `scripts/`, `src/`, `tests/`
and `tools/` carry SPDX headers, as does the `nsx.yml` module manifest at the
repository root; the exception is `tools/release/templates/`, whose files are
rendered into customer drops. The files under `src/generated/` do not: they are
produced by `tools/as7058_json_to_profile.py` from ams-OSRAM AS7058
configuration presets, are not Ambiq-authored, and carry no header; see
THIRD-PARTY-NOTICES.md for the ams-OSRAM terms that accompany the AS7058
material in this repository.

The board definitions under `boards/` do not carry headers either: they are
`kind: packaged` modules vendored by `nsx sync` from the packaged neuralspotx
board modules, and their on-disk content is hashed against `nsx.lock`, so a
header added here would be overwritten on the next sync and would fail
`nsx sync --frozen`. Those headers belong upstream in the neuralspotx packages.
